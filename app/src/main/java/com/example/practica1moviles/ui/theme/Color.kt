package com.example.practica1moviles.ui.theme

import androidx.compose.ui.graphics.Color

// Paleta Dark Elegante
val BackgroundBase = Color(0xFF0B0D10)
val BackgroundGradientStart = Color(0xFF0A0D12)
val BackgroundGradientMid = Color(0xFF10151B)
val BackgroundGradientEnd = Color(0xFF0A1F1A)

// Acento primario - Emerald
val EmeraldPrimary = Color(0xFF10B981)
val EmeraldDark = Color(0xFF059669)

// Textos
val TextPrimary = Color(0xFFEAEAEA)
val TextSecondary = Color(0xFF9BA3AE)

// Cards y superficies
val CardBackground = Color(0xFF111316)
val CardBackgroundAlt = Color(0xFF121417)

// Bordes
val BorderSubtle = Color(0xFFFFFFFF).copy(alpha = 0.12f)
val BorderLight = Color(0xFFFFFFFF).copy(alpha = 0.08f)

// Placeholders y estados
val PlaceholderDark = Color(0xFF1A1F25)
val TextFieldBorderUnfocused = Color(0xFF27303A)

// Estados de interacción
val RippleColor = EmeraldDark.copy(alpha = 0.2f)
val HoverOverlay = EmeraldDark.copy(alpha = 0.3f)
